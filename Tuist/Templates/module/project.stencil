import ProjectDescription

let project = Project.makeModule(
    name: "{{ name }}",
    organizationName: "Newdok",
    bundlePrefix: "com.newdok",
    hasResources: {{ hasResources }},
    hasTests: {{ hasTests }}
)
