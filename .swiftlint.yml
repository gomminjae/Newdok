# SwiftLint Configuration for SwiftUI Project

# 포함할 경로
included:
  - Modules

# 제외할 경로
excluded:
  - Tuist
  - Derived
  - .build
  - "**/Generated"
  - "**/*.generated.swift"
  - "**/Tests"

# 비활성화할 규칙
disabled_rules:
  - trailing_whitespace
  - multiple_closures_with_trailing_closure # SwiftUI에서 trailing closure 패턴 빈번

# 활성화할 Opt-in 규칙
opt_in_rules:
  - array_init
  - attributes
  - closure_end_indentation
  - closure_spacing
  - collection_alignment
  - contains_over_filter_count
  - contains_over_filter_is_empty
  - contains_over_first_not_nil
  - contains_over_range_nil_comparison
  - discouraged_object_literal
  - empty_collection_literal
  - empty_count
  - empty_string
  - enum_case_associated_values_count
  - explicit_init
  - extension_access_modifier
  - fallthrough
  - fatal_error_message
  - first_where
  - flatmap_over_map_reduce
  - identical_operands
  - joined_default_parameter
  - last_where
  - legacy_random
  - literal_expression_end_indentation
  - lower_acl_than_parent
  - modifier_order
  - operator_usage_whitespace
  - overridden_super_call
  - override_in_extension
  - pattern_matching_keywords
  - prefer_self_type_over_type_of_self
  - reduce_into
  - redundant_nil_coalescing
  - redundant_type_annotation
  - sorted_first_last
  - static_operator
  - toggle_bool
  - unavailable_function
  - unneeded_parentheses_in_closure_argument
  - untyped_error_in_catch
  - vertical_parameter_alignment_on_call
  - vertical_whitespace_closing_braces
  - vertical_whitespace_opening_braces
  - xct_specific_matcher
  - yoda_condition

# Analyzer 규칙
analyzer_rules:
  - explicit_self
  - unused_declaration
  - unused_import

# 규칙 상세 설정
attributes:
  always_on_same_line:
    - "@objc"

closure_body_length:
  warning: 30
  error: 100

collection_alignment:
  align_colons: false

cyclomatic_complexity:
  warning: 10
  error: 20

file_length:
  warning: 500
  error: 1000
  ignore_comment_only_lines: true

force_cast: warning
force_try: warning

function_body_length:
  warning: 60
  error: 100

function_parameter_count:
  warning: 5
  error: 8

identifier_name:
  min_length:
    warning: 2
    error: 1
  max_length:
    warning: 50
    error: 60
  excluded:
    - id
    - URL
    - url
    - x
    - y
    - i
    - j

line_length:
  warning: 120
  error: 200
  ignores_urls: true
  ignores_function_declarations: true
  ignores_comments: true

modifier_order:
  preferred_modifier_order:
    - acl
    - setteracl
    - override
    - dynamic
    - mutators
    - lazy
    - final
    - required
    - convenience
    - typeMethods
    - owned

nesting:
  type_level:
    warning: 2
    error: 3
  statement_level:
    warning: 5
    error: 10

number_separator:
  minimum_length: 5

type_body_length:
  warning: 300
  error: 500

type_name:
  min_length: 3
  max_length:
    warning: 50
    error: 60
  allowed_symbols: []
  validates_start_with_lowercase: error

vertical_whitespace:
  max_empty_lines: 1

# 커스텀 규칙
custom_rules:
  comments_space:
    name: "Space After Comment"
    regex: "(^ *//\\w+)"
    message: "There should be a space after //"
    severity: warning

# Reporter 타입
reporter: "xcode"
